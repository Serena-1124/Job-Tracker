# 简历智能匹配工具 - 环境配置说明

## ⚠️ 重要提示：需要安装Node.js环境

我注意到当前系统中**未检测到Node.js运行环境**。为了继续开发简历智能匹配工具，需要先配置Node.js环境。

## 为什么需要Node.js？

简历智能匹配工具采用现代化的前端技术栈构建：

- **React 18** - 用于构建用户界面的JavaScript框架
- **Vite 5** - 极速的前端构建工具
- **TypeScript** - 提供类型安全的开发体验
- **Tailwind CSS** - 原子化CSS样式系统

这些技术都依赖于Node.js运行时环境才能运行。

## 如何安装Node.js

### 方法一：官网下载安装包（最简单，推荐）

1. **访问Node.js官网**
   - 打开浏览器访问：https://nodejs.org/
   
2. **下载LTS版本**
   - 点击绿色的" LTS"按钮下载Windows安装包
   - 推荐下载 **Node.js 20.x LTS**（长期支持版，稳定可靠）
   
3. **运行安装程序**
   - 双击下载的`.msi`安装包
   - 按照安装向导提示操作
   - **重要**：勾选"Add to PATH"选项
   - 使用默认安装路径即可

4. **验证安装**
   - 安装完成后，**重新打开一个新的命令行窗口**（重要！）
   - 运行以下命令验证：
   ```bash
   node --version
   npm --version
   ```
   - 如果看到版本号（如 `v20.10.0` 和 `10.x.x`），说明安装成功

### 方法二：使用Windows包管理器（winget）

如果你使用的是Windows 10/11且系统已内置winget，可以：

1. **打开PowerShell**（以管理员身份）
2. **运行以下命令**：
   ```powershell
   winget install OpenJS.NodeJS.LTS
   ```
3. **重启终端**后验证安装

### 方法三：使用Chocolatey（如果你有Chocolatey）

```powershell
choco install nodejs-lts
```

## 安装后操作

Node.js安装成功后，请运行以下命令确认环境正常：

```bash
node --version
npm --version
```

**预期输出示例**：
```
v20.10.0
10.9.0
```

## 下一步计划

确认Node.js环境可用后，我将立即为你：

1. ✅ **初始化React+Vite项目** - 使用现代化前端技术栈
2. ✅ **配置Tailwind CSS** - 构建精美的用户界面
3. ✅ **实现简历解析功能** - 智能解析DOCX文档
4. ✅ **实现OCR文字识别** - 支持截图上传识别JD
5. ✅ **实现智能匹配算法** - 分析经历与JD的匹配度
6. ✅ **实现AI润色功能** - 自动优化简历表达
7. ✅ **实现Word导出** - 一键生成定制简历文档
8. ✅ **完整UI界面** - 流畅的用户体验

## 技术亮点

- 🛡️ **隐私优先**：所有简历处理在本地完成，不上传服务器
- ⚡ **极速体验**：Vite构建，毫秒级热更新
- 🎨 **精美设计**：Tailwind CSS打造的专业视觉体验
- 📱 **响应式布局**：适配桌面、平板、手机全设备
- 🔧 **功能完整**：从JD输入到简历导出，完整闭环

---

**请先安装Node.js（大约2-3分钟），安装完成后告诉我，我将立即开始开发！** 🚀

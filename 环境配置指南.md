# 简历智能匹配工具 - 环境配置指南

## 当前环境状态

**问题**：检测到当前系统中未安装Node.js和npm，这是运行现代前端项目所必需的环境。

## Node.js 安装指南

### 推荐安装方式

#### 1. 通过官网下载安装包（推荐）

访问 Node.js 官方网站下载安装包：

- **下载地址**：https://nodejs.org/
- **推荐版本**：Node.js 20.x LTS（长期支持版本）
- **步骤**：
  1. 点击"Downloads"页面
  2. 选择 Windows Installer (.msi) 版本
  3. 下载完成后双击运行安装包
  4. 按照安装向导完成安装（建议使用默认配置）
  5. 安装完成后，重新打开终端，输入 `node --version` 验证安装

#### 2. 通过winget安装（Windows用户）

如果你使用的是Windows 10/11且已安装winget，可以在PowerShell中运行：

```powershell
winget install OpenJS.NodeJS.LTS
```

#### 3. 通过Chocolatey安装

如果你已安装Chocolatey包管理器，可以运行：

```powershell
choco install nodejs-lts
```

## 验证安装

安装完成后，打开新的终端窗口，运行以下命令验证：

```bash
# 检查Node.js版本
node --version

# 检查npm版本
npm --version
```

如果能看到版本号（如 `v20.10.0` 和 `10.x.x`），说明安装成功。

## 安装完成后的下一步

Node.js安装成功后，我将继续为你完成以下工作：

1. ✅ 初始化React+Vite项目
2. ✅ 配置Tailwind CSS样式系统
3. ✅ 实现简历解析服务（DOCX解析）
4. ✅ 实现OCR文字识别服务
5. ✅ 实现智能匹配算法
6. ✅ 实现AI润色功能
7. ✅ 实现Word文档导出
8. ✅ 构建完整的用户界面
9. ✅ 测试所有功能

## 技术架构预览

本项目将采用以下技术栈：

- **前端框架**：React 18 + TypeScript
- **构建工具**：Vite 5
- **样式系统**：Tailwind CSS 3
- **状态管理**：Zustand
- **文档解析**：mammoth.js（DOCX解析）
- **OCR识别**：Tesseract.js（本地OCR）
- **文档导出**：docx库

所有简历处理将在浏览器本地完成，确保你的隐私安全。

---

**请先安装Node.js，安装完成后告诉我，我将立即开始开发工作！**
